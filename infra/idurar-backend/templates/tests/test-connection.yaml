apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "idurar-backend.fullname" . }}-test-connection"
  labels:
    {{- include "idurar-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "idurar-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
