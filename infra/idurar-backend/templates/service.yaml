apiVersion: v1
kind: Service
metadata:
  name: {{ include "idurar-backend.fullname" . }}
  labels:
    {{- include "idurar-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "idurar-backend.selectorLabels" . | nindent 4 }}
